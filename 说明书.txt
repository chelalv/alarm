使用的是11脚GPIO0作为PIR口
5V风扇：VCC    PIN4
        GND    PIN6
      
雷达：  VCC3.3 PIN1
        GND    PIN9
        OT2    PIN13

        
热成像：VCC 5v  PIN4/PIN2
        GND    PIN6/PIN14
        SDA    PIN3
        SCL    PIN5
'''       
热成像2 VCC3.3 PIN17
        GND    PIN20
        SDA    PIN27
        SCL    PIN28
PIR     VCC5v  PIN
        GND    PIN9
        OUT    PIN11
'''
        
热成像传感器型号是 MLX90640 D55 MLX90640红外热像仪模块 32×24像素 110度视场角 I2C接口
https://www.waveshare.net/wiki/MLX90640-D55_Thermal_Camera#Raspberry_Pi_4B
不是MLX90641红外热像仪模块 16×12像素



点击微信界面上面的控制台，然后把默认的关闭提示音取消就行啦。

火焰传感器可以检测到打火机，蜡烛火焰，但是检测不到燃气火焰
热成像可以检测到高温，但是有可能煤气关了，锅很热，有可能会误报,目前没有这个问题
PIR传感器需要人走动才会检测到，如果不动，也不会检测到
1分钟之内检测到有人动，就算有人，1分钟没有人接近，就算没有人

这个PIR传感器每隔1分钟左右自己会跳到一次，买另外厂家的再测试
新买的PIR也有类似的问题，莫名其妙也会报有人，这个问题就比较严重，可以漏报，但是不能错报

使用24G毫米波雷达可以检测有人没人，但是需要在启动的时候就读一下
GPIO值，要不然如果已经有人在，就不会产生中断来提示有人
在使用软件生成阈值的时候，不能动作太大，要不然很容易产生检测不人，
也不能一动不动，这样就很容易检测到人


使用小米插座，设置早上打开电源，晚上关闭电源

设置树莓派开机启动程序 systemd
https://forums.raspberrypi.com/viewtopic.php?p=921354#p921354
sudo vim /etc/systemd/system/alarm.service
[Service]
WorkingDirectory=/home/matt/projects/alarm/code
ExecStart=python ./alarm.py
Restart=always
StandardOutput=journal
StandardError=journal
SyslogIdentifier=alarm
User=matt
Group=audio
Environment=NODE_ENV=production
Environment="XDG_RUNTIME_DIR=/run/user/1000"
Environment="PULSE_RUNTIME_PATH=/run/user/1000/pulse/"
[Install]
WantedBy=multi-user.target

sudo systemctl enable alarm.service
还需要设置一下开机启动的默认target：
matt@rpi4-2:~ $ systemctl get-default
multi-user.target
matt@rpi4-2:~ $ sudo systemctl set-default graphical.target
Removed "/etc/systemd/system/default.target".
Created symlink /etc/systemd/system/default.target → /lib/systemd/system/graphical.target.
matt@rpi4-2:~ $ sudo reboot



/etc/crontab 设置自动关机，每天晚上关机
00 20   * * *   root    shutdown -h now

在service uint文件里，要写python ./alarm.py，而不是 python alarm.py.
我测试了
sudo crontab -e
In crontab add this line
@reboot /sbin/shutdown -h 20:00
ctrl+X to save and install the crontab.
这个办法也还是一样的问题
树莓派4上没有这个问题，感觉是poweroff之后，又启动了一次

增加每小时心跳一次+CPU温度记录在log里

原子变量 pip install atomic，但是没有boolean类型

检测到高温的时候，增加温度打印
CPU：cat /sys/class/thermal/thermal_zone0/temp
GPU：vcgencmd measure_temp

EOFError: EOF when reading a line 这是使用input的时候，但是程序又是后台运行的时候会出现
使用try/except解决


设置ssh自动登录rpi：
https://www.tinkerboy.xyz/how-to-setup-putty-to-automatically-login-to-your-raspberry-pi/

增加喇叭播放报警

有时候会莫名其妙连续检测到高温，其实这时候已经没有高温了
这个问题使用微雪模块会遇到，使用MCU版本的模块没有了


RPI4部署：
1. 播放m4a 
    sudo apt install mplayer 
2. 解决热传感器 cnt > 4 error 
sudo vim /boot/firmware/config.txt
dtparam=i2c_arm=on,i2c_arm_baudrate=400000
3. lgpio.error: 'GPIO busy'
#RPI4使用13脚GPIO 27
pir = DigitalInputDevice(pin = 27)
4. sudo crontab -e
In crontab add this line
@reboot /sbin/shutdown -h 20:00
5. sudo vim /etc/systemd/system/alarm.service
6. mplayer没有声音
https://askubuntu.com/questions/1379890/how-to-play-a-sound-from-a-systemd-service-on-ubuntu
Environment="XDG_RUNTIME_DIR=/run/user/1000"
Environment="PULSE_RUNTIME_PATH=/run/user/1000/pulse/"
7. I2C Read Error!
I2C的线不能太长，换成20cm
8. journalctl -u alarm.service -f 查看log，这里printf和log都会记录
9. sudo i2cdetect -y 1 会出现所有的地址
